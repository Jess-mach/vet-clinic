{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ESBUILD_BINARY_PATH=/tmp/esbuild node node_modules/vite/bin/vite.js",
    "build": "ESBUILD_BINARY_PATH=/tmp/esbuild tsc -b && vite build",
    "lint": "eslint .",
    "preview": "ESBUILD_BINARY_PATH=/tmp/esbuild vite preview"
  }
}
